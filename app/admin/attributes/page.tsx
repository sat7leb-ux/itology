export default function Page() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold">Attributes</h1>
      <div className="mt-6 rounded-md border border-line bg-white p-10 text-center">
        <p className="text-sm text-text-muted">Define reusable attributes (e.g. CPU, RAM, Refresh Rate) and attach them to categories to drive dynamic filters and spec sheets.</p>
      </div>
    </div>
  );
}
